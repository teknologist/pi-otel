# Aspire dashboard

Start the dashboard from pi:

```
/otel start
```

Dashboard UI → <http://localhost:18888>

The `/otel start` command auto-detects the `aspire` CLI, `docker`, or `podman`.
Use `--driver=` to force one: `/otel start --driver=docker`.
